#!/usr/bin/env bash
# Versioned git hook (Vademecum §5). Hooks call just recipes only.
set -euo pipefail
if command -v mise >/dev/null 2>&1; then
  exec mise exec -- just prepush
else
  exec just prepush
fi
